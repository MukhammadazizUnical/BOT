global:
  resolve_timeout: 5m

route:
  receiver: send-bot-webhook
  group_by: [alertname, service]
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 5m

receivers:
  - name: send-bot-webhook
    webhook_configs:
      - url: http://app:3010/alerts/prometheus
        send_resolved: false
