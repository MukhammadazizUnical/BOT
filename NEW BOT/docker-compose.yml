services:
  app:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      - BOT_ROLE=app
      - TZ=UTC-5
    ports:
      - '3011:3010'
    depends_on:
      - postgres
    command: uvicorn app.main:app --host 0.0.0.0 --port 3010

  worker:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      - BOT_ROLE=worker
      - TZ=UTC-5
    depends_on:
      - postgres
    command: arq app.worker.WorkerSettings

  bot:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      - BOT_ROLE=app
      - TZ=UTC-5
    depends_on:
      - postgres
    command: python -m app.bot_runner

  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=1111
      - POSTGRES_DB=tgbot
      - TZ=UTC-5
      - PGTZ=UTC-5
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
